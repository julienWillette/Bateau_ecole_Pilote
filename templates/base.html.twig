<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content={% block metadescription %}{% endblock %} />
    <title>Bateau École Pilote{% block title %}{% endblock %}</title>
    <link rel="icon" type="image/png" href="https://zupimages.net/up/21/04/2zj1.png" />
    <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@300&display=swap" rel="stylesheet">
    <link rel="icon" type="image/png" href="https://zupimages.net/up/21/04/2zj1.png" />
    <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@300&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Oxygen&family=Roboto:wght@700&display=swap" rel="stylesheet">
    {{ encore_entry_link_tags('app') }}
    {% block stylesheets %}
    {% endblock %}
</head>

<body>
    <header>
        <nav class="navbar navbar-expand-lg primary-color nav-cart">
            <a class="navbar-brand" href="{{ path('home')}}"><img class="logo-nav"
                    src="https://zupimages.net/up/21/04/2zj1.png" alt="navbar logo">
            </a>
            <button class="navbar-toggler ml-auto" type="button" data-toggle="collapse" data-target="#basicExampleNav"
                aria-controls="basicExampleNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon fixed-top ml-auto"><i class="fas fa-bars"></i></span>
            </button>
            <div class="collapse navbar-collapse" id="basicExampleNav">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item active">
                        <a class="nav-link " href="{{ path('home')}}">Accueil
                            <span class="sr-only">(current)</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('license_index') }}">Formation</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('contact')}}">Contact</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('blog')}}">Blog</a>
                    </li>
                    {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin')}}">Admin</a>
                    </li>
                    {% endif %}
                    {% if is_granted("ROLE_USER")%}
                    {% if app.user.isActivated == 1 %}
                    {% if not is_granted("ROLE_ADMIN")%}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('profile') }}">Profil</a>
                    </li>
                    {% endif %}
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout')}}"><i class="fas fa-sign-out-alt"></i></a>
                    </li>
                    {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login')}}">Connexion</a>
                    </li>
                    {% endif %}
                </ul>
            </div>
        </nav>
        {% if is_granted("ROLE_USER")%}
        {% if app.user.isActivated == 0 %}
        <div class="alert alert-success" role="alert">
            Merci de vérifier vos mails pour activer votre compte (pensez à regarder le dossier spam) !
        </div>
        {% endif %}
        {% endif %}
        <div class="menu">
            <nav class="menu__nav">
                <ul class="menu__list r-list">
                    <li class="menu__group">
                        <a class="menu__link r-link " href="{{ path('home')}}">Accueil
                            <span class="sr-only">(current)</span>
                        </a>
                    </li>
                    <li class="menu__group">
                        <a class="menu__link r-link" href="{{ path('license_index') }}">Formation</a>
                    </li>
                    <li class="menu__group">
                        <a class="menu__link r-link" href="{{ path('contact')}}">Contact</a>
                    </li>
                    <li class="menu__group">
                        <a class="menu__link r-link" href="{{ path('blog')}}">Blog</a>
                    </li>
                    {% if is_granted("ROLE_ADMIN") %}
                    <li class="menu__group">
                        <a class="menu__link r-link" href="{{ path('admin')}}">Admin</a>
                    </li>
                    {% endif %}
                    {% if is_granted("ROLE_USER")%}
                    {% if app.user.isActivated == 1 %}
                    {% if not is_granted("ROLE_ADMIN")%}
                    <li class="menu__group">
                        <a class="menu__link r-link" href="{{ path('profile') }}">Profil</a>
                    </li>
                    {% endif %}
                    {% endif %}
                    <li class="menu__group">
                        <a class="menu__link r-link" href="{{ path('app_logout')}}"><i
                                class="fas fa-sign-out-alt"></i></a>
                    </li>
                    {% else %}
                    <li class="menu__group">
                        <a class="menu__link r-link" href="{{ path('app_login')}}">Connexion</a>
                    </li>
                    {% endif %}
                </ul>
            </nav>
            <button class="menu__toggle r-button" type="button">
                <span class="menu__hamburger m-hamburger">
                    <span class="m-hamburger__label">
                        <span class="menu__screen-reader screen-reader">Open menu</span>
                    </span>
                </span>
            </button>
        </div>
    </header>
    {% block body %}{% endblock %}
    <footer class="footer footer-bg d-none d-lg-block pb-4">
        <div class="container justify-fluid-content-around">
            <div class="row align-items-center">
                <div class="col-lg-4 text-center">
                    <div class="col-md-12">
                        <a class="footer-brand" href="{{ path('home')}}"><img class="logo-footer"
                                src="https://zupimages.net/up/21/04/2zj1.png" alt="navbar logo">
                            <div class="container-fluid">
                                <a class="space" href="tel:{{ info.infoFooter[0].phone}}"><img
                                        src="https://zupimages.net/up/20/51/td5h.png" class="reseau-footer"
                                        alt="logo-phone"></a>
                                <a class="space" href="mailto:{{ info.infoFooter[0].mail }}"><img
                                        src="https://zupimages.net/up/20/51/yvjh.png" class="reseau-footer"
                                        alt="logo-mail"></a>
                            </div>
                    </div>
                </div>
                <div class="col-lg-4 mt-4 text-center">
                    <h3 class="pt-3 information-style">Informations</h3>
                    <hr class="hr-color">
                    <div class="row">
                        <div class="col-md-12">
                            <a class="footer-information" href="{{ path('contact')}}">Contact</a>
                            <br>
                            <a class="footer-information" href="{{ path('faq')}}">Faq</a>
                        </div>
                    </div>
                </div>
                <div class="col-lg-4 text-center">
                    <h3 class="pt-3 information-style">Nous suivre</h3>
                    <hr class="hr-color">
                    <div class="row">
                        <div class="col-md-12">
                            <a class="space" href="https://www.facebook.com/ecoledeconduite.pilote.3"><img
                                    src="https://zupimages.net/up/20/51/lxhz.png" class="reseau-footer"
                                    alt="logo-facebook"></a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div style="height: 50px"></div>
    </footer>
    <script src="https://kit.fontawesome.com/eacfbebdea.js" crossorigin="anonymous"></script>
    {{ encore_entry_script_tags('app') }}
    {% block javascripts %}
    {% endblock %}
</body>

</html>