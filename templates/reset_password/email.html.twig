<div class="container mailPwd">

    <h1 class="mailTitle">Renouvellement de votre mot de passe</h1>

    <p>Afin de renouveler votre mot de passe, merci de cliquer sur le lien ci-dessous.</p>

    <a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token:
        resetToken.token}) }}</a>

    <p>Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData,
        'ResetPasswordBundle') }}.</p>

    <p>Cordialement</p>
</div>